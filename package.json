{
  "name": "playwright-pom-bdd",
  "version": "1.0.0",
  "scripts": {
    "test:chrome": "set BROWSER=chrome && cucumber-js --require-module ts-node/register --require src/support/world.ts --require src/steps/**/*.ts --format json:reports/cucumber_report.json",
    "test:edge": "set BROWSER=msedge && cucumber-js --require-module ts-node/register --require src/support/world.ts --require src/steps/**/*.ts",
    "test:merge": "node scripts/merge-cucumber.js",
    "test:report": "npx cucumber-html-reporter --config cucumber-html.config.js",
    "test:all": "npm run test:chrome && npm run test:edge && npm run test:merge && npm run test:report && npm run test:api && npm run test",
    "test": "npm run test:ui && npm run test:api",
    "test:ui": "cucumber-js --format json:reports/cucumber_report.json",
    "clean:api": "rimraf test-results/api",
    "clean:test-results": "rimraf test-results",
    "test:api": "npx playwright test --project=api",
    "report:api": "npx playwright show-report reports",
    "test:chrome:report": "npm run test:chrome && npm run report",
    "test:ui:report": "node generate-report.js",
    "open:ui-report": "start reports\\cucumber_report.html",
    "test:api:report": "npx playwright show-report reports/api",
    "report": "node generate-report.js",
    "cucumber:dry": "cucumber-js --dry-run --require-module ts-node/register --require src/steps/**/*.ts"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "@playwright/test": "^1.38.0",
    "cucumber-html-reporter": "^7.2.0",
    "rimraf": "^6.0.1",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
